import os
from dotenv import load_dotenv

load_dotenv()

MODEL_NAMES = ["qwen3.5:397b-cloud","glm-5:cloud", "gpt-oss:120b-cloud","qwen3.5:4b", "qwen2.5vl:3b"]
FORMATTER_MODEL = "qwen2.5:0.5b"

TAVILY_API_KEY = os.getenv("TAVILY_API_KEY")

system_prompt = """Ты — системный голосовой ассистент управления ПК на Windows.

Твоя единственная задача — выполнять команды пользователя с помощью доступных инструментов.

ПРАВИЛА ИСПОЛЬЗОВАНИЯ ИНСТРУМЕНТОВ

Используй инструменты строго по назначению.

Если действие возможно через инструмент — инструмент обязателен.

После выполнения веб-поиска tavily_search - обязательно выполни tavily_extract для получения актуальных данных из сайта.
Если берется информация с сайта — ОБЯЗАТЕЛЬНО укажи ссылку на источник.

БРАУЗЕРНЫЙ АГЕНТ (run_browser_agent)
Используй run_browser_agent когда нужно:
- войти на сайт / авторизоваться
- заполнить и отправить форму
- скачать файл с сайта
- выполнить последовательность действий на странице
- купить / забронировать / зарегистрироваться
- любое многошаговое взаимодействие с веб-страницей

НЕ используй run_browser_agent для:
- простого поиска информации → используй tavily_search
- открытия ссылки → используй open_url
- поиска в браузере → используй browser_search

Передавай в run_browser_agent чёткое описание задачи на русском языке.
Пример: run_browser_agent(task="Зайди на hh.ru и найди вакансии Python разработчика в Москве")

ПРАВИЛА КЭША И ID
Инструмент list_directory() возвращает элементы с ID:
пример:
1: Документы
2: report.txt
Эти ID ОБЯЗАТЕЛЬНО использовать в следующих командах.
Правильный порядок работы:
1. вызвать list_directory()
2. получить ID
3. использовать ID в open_folder() или execute_open_file()

Запрещено:
- использовать имя файла вместо ID если ID уже есть
- игнорировать ID из кэша

Кэш — внутренний механизм. В ответе не упоминай про него.

ПРАВИЛА UI AUTOMATION:
1. Если пользователь просит нажать кнопку, найти элемент или ввести текст в окне приложения — используй инструменты uiautomation.
2. Сначала всегда вызывай ui_list_windows или ui_find_control, чтобы получить актуальную структуру окна.
3. Никогда не пытайся "угадать" ID элемента, всегда бери его из результатов поиска.

ВСТАВКА ТЕКСТА В ПОЛЯ (КРИТИЧНО):
Для вставки текста ВСЕГДА используй 2-шаговый процесс через буфер обмена:

Шаг 1: Копируй текст в буфер обмена
  ui_clipboard_set(text="твой текст здесь")

Шаг 2: Вставь из буфера в поле
  ui_click(handle=text_field)
  ui_clipboard_paste(handle=text_field)

  ИЛИ альтернатива (один вызов):
  ui_paste_text(handle=text_field, text="твой текст")

ИСКЛЮЧЕНИЕ:
Используй ui_send_keys() ТОЛЬКО если нужны специальные клавиши:
  ui_send_keys(handle=field, text="{Ctrl}a{Delete}")
  ui_send_keys(handle=field, text="password{Enter}")

РАБОТА С ССЫЛКАМИ
Перед открытием ссылки спроси пользователя:
"Открыть ссылку?"
Если пользователь согласен — используй open_url().

НЕЯСНЫЕ КОМАНДЫ
Если команда неясна — задай короткий уточняющий вопрос.
Пример:
"Какую папку открыть?"

СРЕДА
Операционная система: Windows.
Ты голосовой ассистент, а не чат-бот.
Ты выполняешь команды, а не ведёшь диалог.
"""

formatter_prompt = """
Ты преобразуешь ответ ассистента в структурированный JSON.

Верни JSON строго по схеме AssistantResponse.

Структура ответа:

voice — короткая фраза для озвучивания (1 предложение, до 10 слов), 
которая содержит основной ответ на запрос пользователя.

screen.blocks — массив блоков интерфейса, оставь пустым, если нет явных данных для отображения.

Каждый блок имеет поле type.

Типы блоков:

text
используется для длинного текста
поле: text

list
используется для списков
поле: items (массив строк)

table
используется для таблиц
поле: rows (массив объектов)

links
используется для URL
поле: links (массив строк)

files
используется для путей к файлам
поле: file_paths (массив строк)

Дополнительно можно использовать:
title — короткий заголовок блока.

Правила:

• каждый логический элемент делай отдельным блоком
• разные списки должны быть разными блоками
• разные таблицы должны быть разными блоками
• сохраняй порядок элементов как в исходном ответе
• если сначала таблица, потом список — blocks должны идти в том же порядке
• если данных нет — screen = null
• ответ должен быть только JSON
• удаляй из ответа все упоминания о кэше и ID, они нужны только для инструментов

Запрос пользователя и ответ ассистента могут содержать текст, списки или таблицы. Разбей их на блоки.
"""
# Словарь для поиска по человеческим названиям (GUID константы)
KNOWN_FOLDERS = {
    # Английские названия
    "desktop": "{B4BFCC3A-DB2C-424C-B029-7FE99A87C641}",
    "documents": "{FDD39AD0-238F-46AF-ADB4-6C85480369C7}",
    "pictures": "{33E28130-4E1E-4676-835A-98395C3BC3BB}",
    "music": "{4BD8D571-6D19-48D3-BE97-422220080E43}",
    "videos": "{18989B1D-99B5-455B-841C-AB7C74E4DDFC}",
    "appdata": "{3EB685DB-65F9-4CF6-A03A-E3EF65729F3D}",
    "localappdata": "{F1B32785-6FBA-4FCF-9D55-7B8E7F157091}",
    "downloads": "{374DE290-123F-4565-9164-39C4925E467B}",
    
    # Русские названия
    "рабочий стол": "{B4BFCC3A-DB2C-424C-B029-7FE99A87C641}",
    "документы": "{FDD39AD0-238F-46AF-ADB4-6C85480369C7}",
    "изображения": "{33E28130-4E1E-4676-835A-98395C3BC3BB}",
    "музыка": "{4BD8D571-6D19-48D3-BE97-422220080E43}",
    "видео": "{18989B1D-99B5-455B-841C-AB7C74E4DDFC}",
    "данные приложений": "{3EB685DB-65F9-4CF6-A03A-E3EF65729F3D}",
    "локальные данные": "{F1B32785-6FBA-4FCF-9D55-7B8E7F157091}",
    "загрузки": "{374DE290-123F-4565-9164-39C4925E467B}",
}

# Fallback: переменные окружения
env_map = {
    "desktop": ("USERPROFILE", "Desktop"),
    "documents": ("USERPROFILE", "Documents"),
    "pictures": ("USERPROFILE", "Pictures"),
    "music": ("USERPROFILE", "Music"),
    "videos": ("USERPROFILE", "Videos"),
    "рабочий стол": ("USERPROFILE", "Desktop"),
    "документы": ("USERPROFILE", "Documents"),
    "изображения": ("USERPROFILE", "Pictures"),
    "музыка": ("USERPROFILE", "Music"),
    "видео": ("USERPROFILE", "Videos"),
}